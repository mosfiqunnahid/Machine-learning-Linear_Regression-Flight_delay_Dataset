# -*- coding: utf-8 -*-
"""Linear_Regression-Flight_delay_Dataset.ipynb

Automatically generated by Colaboratory.

Original file is located at
    https://colab.research.google.com/drive/18QU1EEt8EckzyI43JCfFtmw3WlZKdKdA
"""

import numpy as np 
import pandas as pd   
from sklearn import metrics
import matplotlib.pyplot as plt  
import seaborn as seabornInstance 
import seaborn as sns; sns.set(style='ticks')
from sklearn.linear_model import LinearRegression
from sklearn.model_selection import train_test_split

data = pd.read_csv('/content/airlinedelaycauses_DelayedFlights.csv')
data.shape
null_data=data
print(null_data)

corr = data.corr()
mask = np.triu(np.ones_like(corr, dtype=np.bool))
f, ax = plt.subplots(figsize=(11, 9))
cmap = sns.diverging_palette(210, 10, as_cmap=True)
sns.heatmap(corr, mask=mask, cmap=cmap, vmax=.3, center=0,
            square=True, linewidths=.5, cbar_kws={"shrink": .5})

null_data=null_data.replace(np.nan,0)
null_data

data0 = null_data.iloc[:,:].values
data0 = data0[:,:]
y = data0[:,29]
x = data0[:,[5,6,7,8,10,12,13,14,15,16,19,20,21,25,26,27,28]]
print(y)
print(x)

X_train, X_test, y_train, y_test = train_test_split(x, y, test_size=0.2)

regressor = LinearRegression()  
regressor.fit(X_train, y_train)
print(X_train)

y_pred = regressor.predict(X_test)

df = pd.DataFrame({'Actual': y_test.flatten(), 'Predicted': y_pred.flatten()})
df

plt.scatter(y_pred, y_test)

metrics.r2_score(y_test, y_pred)